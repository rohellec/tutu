<div class="col-5">
  <h1><%= t(".edit_header") %></h1>

  <%= render "form", ticket: @ticket %>

  <%= link_to t("show"), admin_ticket_path(@ticket) %> |
  <%= link_to t("back"), admin_tickets_path %>
</div>
