<div class="col-5">
  <h1><%= t(".edit_header") %></h1>

  <%= form_for @wagon, url: admin_wagon_path(@wagon) do |form| %>
    <%= render "common/errors", resource: @wagon %>
    <%= render "fields", wagon: @wagon %>
  <% end %>

  <%= link_to t('show'), admin_wagon_path(@wagon) %> |
  <%= link_to t('back'), admin_train_path(@wagon.train) %>
</div>
