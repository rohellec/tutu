<h1>New Wagon</h1>

<%= form_for [:admin, @train, @wagon] do |form| %>
  <%= render "common/errors", resource: @wagon %>
  <%= render "fields", wagon: @wagon %>
<% end %>

<%= link_to 'Back', [:admin, @train] %>
