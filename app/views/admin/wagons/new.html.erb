<h1>New Wagon</h1>

<%= form_for @wagon, url: admin_train_wagons_path(@train) do |form| %>
  <%= render "common/errors", resource: @wagon %>
  <%= render "fields", wagon: @wagon %>
<% end %>

<%= link_to 'Back', admin_train_path(@train) %>
